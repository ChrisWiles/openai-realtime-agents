// Central re-exports so agent files don’t need to reach deep into the SDK path

export type { FunctionTool, RealtimeAgent } from '@openai/agents/realtime';
export { tool } from '@openai/agents/realtime';
